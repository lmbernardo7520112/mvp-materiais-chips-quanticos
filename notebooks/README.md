# Notebooks

> **Status:** Deferidos para v0.2.

Os notebooks Jupyter (`.ipynb`) serão derivados dos scripts em `scripts/` na versão v0.2.

Na versão v0.1, toda a lógica computacional está nos módulos Python em `src/mvp_quantum_materials/`
e é exercitada pelos scripts CLI em `scripts/`.

Os notebooks não conterão lógica principal (RNF-07) — servirão apenas para
visualização e exploração interativa dos resultados.
