# v0.8.8 RED Risk Matrix

| Risk | Severity | Failure Mode | Mitigation | Gate |
| :--- | :--- | :--- | :--- | :--- |
| **R1 — RED accidentally includes implementation** | Critical | Source module created | G2, G7: target module absent, zero src changes | G2, G7 |
| **R2 — tests import solver runtime** | Critical | Test imports `solve_poisson` or `poisson_solver_2d` | Code review; static guard test (cat 8) | G13, G14 |
| **R3 — tests imply physical phi** | High | Test asserts physical phi output | Tests assert `demonstrative_only` status | G15 |
| **R4 — tests imply calibrated Poisson** | High | Test accepts `calibration_status: calibrated` | Test explicitly rejects calibration | G18 |
| **R5 — tests imply device prediction** | High | Test accepts `device_prediction_enabled: True` | Test explicitly rejects device prediction | G19 |
| **R6 — naming implies potential/voltage output** | High | Tests use `phi`, `voltage`, `potential` as physical quantities | Forbidden names set enforced in cat 5 tests | G15, G17 |
| **R7 — generate_all_results modified prematurely** | Medium | Script references one_way_poisson_coupling | Static guard test (cat 9); G8, G27 | G8, G27 |
| **R8 — CI red misinterpreted as project failure** | Medium | RED failures treated as regression | PR body and docs clearly state RED-only | G28 |
| **R9 — scope creep into GREEN** | Critical | Implementation created to make tests pass | No src changes allowed; G7 | G7, G29, G30 |
| **R10 — SDK/API/goal-like execution introduced** | Critical | External services invoked | BudgetOps blocks by default | G20, G21, G22 |
