# v0.8.9 — One-way Poisson Coupling GREEN Plan

## Goal

Implement the minimum non-runtime, non-physical, one-way coupling contract required to pass v0.8.8 RED tests.

## Authorized

* Create `src/mvp_quantum_materials/one_way_poisson_coupling.py`.
* Implement metadata-only adapter contract.
* Implement safety validators.
* Pass the 20 RED tests.
* Add `one_way_poisson_coupling.py` to `authorized_files` in `policy.json`.

## Implementation

* `OneWayPoissonCouplingMetadata` — dataclass with safety-critical defaults.
* `PoissonInputSource` — adapter carrying projected charge data.
* `build_poisson_input_source()` — builder with mandatory boundary/conservation checks.
* `validate_no_physical_phi_claim()` — raises if physical phi claimed.
* `validate_no_solver_runtime_coupling()` — raises if solver coupling claimed.

## Safety Invariants Enforced

* `physical_phi_allowed` must remain `False`.
* `solver_invocation_allowed` must remain `False`.
* `calibration_status` must remain `"not_calibrated"`.
* `device_prediction_enabled` must remain `False`.
* `interpretation_status` is always `"demonstrative_only"`.
* `physical_interpretation` is always `False`.

## Not Authorized

* No Poisson runtime.
* No solver execution.
* No `solve_poisson`.
* No physical phi.
* No dummy phi.
* No potential grid.
* No calibration claims.
* No device prediction.
* No paid API.
* No SDK.
* No `/goal`.
* No goal-like autonomous execution.
