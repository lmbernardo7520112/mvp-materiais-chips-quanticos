# v0.4.9 ADR-009 Future C1 TDD Plan

> **Date:** 2026-05-13
> **Status:** PLAN ONLY — no tests created in this release
> **Skill used:** tdd-red-green-release

---

## Scope

This document lists the minimum RED tests required for a future C1
implementation PR. These tests are **not created now**. They will be
created in the implementation branch after ADR-009 is Accepted.

## Target Module

```
src/mvp_quantum_materials/surface_charge.py    (future, does not exist yet)
```

## Future RED Tests

| # | Test Name | What It Verifies | Expected RED Failure |
|---|-----------|-----------------|---------------------|
| 1 | `test_dit_ev_cm2_to_j_m2_conversion` | D_it × 6.242e22 = D_it_SI | ModuleNotFoundError / ImportError |
| 2 | `test_delta_e_window_must_be_explicit_positive` | Raises if δE_window ≤ 0 or None | ModuleNotFoundError |
| 3 | `test_nit_areal_density_units_and_values` | N_it = D_it_SI × δE_window; N_it ≥ 0 | ModuleNotFoundError |
| 4 | `test_sigma_eff_requires_sign_convention` | s_charge must be ±1, no default | ModuleNotFoundError |
| 5 | `test_occupancy_bounds` | 0 ≤ f_occ ≤ 1; raises otherwise | ModuleNotFoundError |
| 6 | `test_c1_does_not_create_rho_eff` | No ρ_eff function/attribute in C1 module | ModuleNotFoundError |
| 7 | `test_c1_does_not_modify_solver` | poisson_solver_2d.py hash unchanged | PASS (non-regression) |
| 8 | `test_metadata_blocks_physical_phi` | physical_interpretation_allowed = False | PASS (non-regression) |

## Expected RED Phase Result

- 6 tests FAIL (ModuleNotFoundError — module does not exist).
- 2 tests PASS (non-regression guards).
- Total: 8 tests, 6 failures, 2 passes.

## GREEN Phase Requirements

- Create `surface_charge.py` with:
  - `convert_dit_to_si(d_it_ev_cm2: float) -> float`
  - `compute_nit(d_it_si: float, delta_e_window: float) -> float`
  - `compute_sigma_eff(n_it: float, s_charge: int, f_occ: float) -> float`
- All 8 tests pass.
- No solver modification.
- No ρ_eff computation.
- Coverage maintained ≥ 70%.

## Boundary: What C1 GREEN Must NOT Do

- Must NOT import or modify `poisson_solver_2d.py`.
- Must NOT import or modify `effective_charge.py`.
- Must NOT modify `run_poisson_bridge.py`.
- Must NOT change `policy.json`.
- Must NOT set `option_c_enabled = True`.
- Must NOT set `physical_interpretation_allowed = True`.
