# v0.8.7 RED Planning Risk Matrix

| Risk | Severity | Failure Mode | Mitigation | Gate |
| :--- | :--- | :--- | :--- | :--- |
| **R1 — RED planning accidentally becomes RED tests** | Critical | Test files created in `tests/` | G3, G9: zero tests changes, no RED tests created | G3, G9 |
| **R2 — future test names imply physical phi** | High | Test naming uses `physical_phi`, `real_potential` | Code review during future RED PR | G13 |
| **R3 — future tests imply calibrated Poisson** | High | Test asserts calibration accuracy | Non-Authorization Clause bans calibration claims | G18 |
| **R4 — future adapter implies solver coupling** | Critical | Adapter calls `solve_poisson` directly | RED tests must verify solver is NOT callable | G12 |
| **R5 — generate_all_results future artifact looks physical** | High | Future output lacks demonstrative tag | G31: generate_all_results PASS with demonstrative tags | G31 |
| **R6 — C3 guardrails bypassed** | Critical | Future coupling reads raw charge instead of C3 projected | RED test must verify C3 projection is required input | G12 |
| **R7 — BudgetOps omitted in future RED** | High | Future RED PR skips BudgetOps | G23: BudgetOps PASS required | G23 |
| **R8 — Usage Ledger omitted in future RED** | High | Future RED PR skips Usage Ledger | G24: Usage Ledger PASS required | G24 |
| **R9 — human approval bypass** | Critical | Agent merges or tags without human review | Merge/tag require human approval per Autonomy-001 | G34 |
| **R10 — scope creep toward device prediction** | Critical | Planning document implies device-level accuracy | Non-Authorization Clause bans device prediction | G19 |
| **R11 — paid API accidentally introduced** | Critical | Agent calls external model API | BudgetOps blocks paid API by default | G20 |
| **R12 — external SDK accidentally introduced** | Critical | Agent imports external ML/science SDK | BudgetOps blocks external SDK by default | G21 |
| **R13 — goal-like autonomous execution accidentally introduced** | Critical | Agent runs `/goal` or autonomous loop | BudgetOps caps `max_autonomous_goal_loops = 0` | G22 |
| **R14 — future RED includes GREEN implementation** | Critical | Implementation code created during RED phase | RED phase must only contain failing tests | G10, G33 |
| **R15 — future RED creates misleading potential-grid vocabulary** | High | Variable names use `potential_grid`, `voltage_field` | Naming guardrails enforced in future code review | G15, G16, G17 |
