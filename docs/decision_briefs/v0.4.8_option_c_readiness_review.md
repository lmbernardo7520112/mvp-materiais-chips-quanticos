# v0.4.8 Option C Readiness Review

> **Date:** 2026-05-13
> **Type:** Decision Brief — Skills-Governed
> **Skills Used:** physics-dimensional-audit, scope-guardrails, tdd-red-green-release
> **Status:** READINESS REVIEW — no implementation

---

## Context

| Version | Milestone |
|---------|-----------|
| v0.4.4 | SI constants scaffolding (`units.py`, `scale_modes.py`) |
| v0.4.5 | Decision: Option B (metadata-only) recommended; Option C deferred |
| v0.4.6 | Metadata-only runtime declaration implemented in CSV |
| v0.4.7 | Agent Skills governance bootstrap (6 skills) |
| v0.4.8 | **This review** — readiness assessment for Option C |

Option C has never been implemented. The project remains in demonstrative
mode with `physical_interpretation_allowed = False` and
`option_c_enabled = False`.

---

## What Option C Would Mean

Option C would transition the Poisson Bridge from a demonstrative proxy
model to a physically-grounded electrostatic simulation. This requires,
at minimum, the following chain:

### 1. D_it Unit Conversion

```
D_it [eV⁻¹·cm⁻²]  →  D_it_SI [J⁻¹·m⁻²]

D_it_SI = D_it / eV_to_J / (cm_to_m)²
        = D_it × 6.242e18 × 1e4
        = D_it × 6.242e22
```

**Gap:** D_it value selection. Which energy range? Which trap type?
Which temperature? Literature values span orders of magnitude
(10⁹ to 10¹³ eV⁻¹·cm⁻²).

### 2. Energy Integration

```
N_it = ∫ D_it(E) dE  ≈  D_it × δE_window

where δE_window = energy window [J] over which traps contribute
```

**Gap:** `δE_window` is not defined. Choices include:
- Full bandgap (E_g ≈ 1.12 eV for Si) — overestimates.
- kT window — underestimates at low T.
- Specific trap level range — requires experimental data.

### 3. Surface Charge Density

```
σ_eff [C/m²] = q_e × N_it × f(E)

where f(E) = occupancy/sign convention factor
```

**Gap:** Sign convention undefined. Interface traps can be:
- Donor-like (positive when empty, neutral when filled).
- Acceptor-like (neutral when empty, negative when filled).
- The net charge depends on Fermi level position.

### 4. Volume Regularization

```
ρ_eff [C/m³] = σ_eff / t_eff

where t_eff = effective thickness for surface→volume smearing
```

**Gap:** `t_eff` is a regularization parameter, not a physical thickness.
Its value affects ρ_eff by orders of magnitude. Current `t_eff` in
`units.py` is literature-scaled (1 nm) but labeled TX (demonstrative).

### 5. Poisson Source Term

```
∇·(ε ∇φ) = −ρ_eff

where ε = ε_r × ε₀ = 11.7 × 8.854e-12 F/m  (for Si)
```

**Gap:** Current solver uses `epsilon = 1.0` (dimensionless proxy).
Substituting physical ε changes the magnitude of φ by ~10 orders.
The solver's numerical stability with physical ε has not been tested.

### 6. Physical φ Interpretation

```
φ [V] — only meaningful if source, domain, ε, and BCs are all physical
```

**Gap:** `physical_interpretation_allowed` must remain `False` until
all upstream terms are physically justified. Premature enabling would
constitute overclaim.

---

## Readiness Questions

| # | Question | Answer | Blocking? |
|---|----------|--------|-----------|
| 1 | Do we have a justified δE_window? | ❌ No. No convention chosen; no sensitivity analysis. | **Yes** |
| 2 | Do we have a charge sign convention for traps? | ❌ No. Donor-like vs acceptor-like not specified. | **Yes** |
| 3 | Do we have a justification for t_eff as regularization? | ⚠️ Partial. t_eff = 1 nm exists in `units.py` but is tier TX. | **Yes** |
| 4 | Do we have physical geometry or only normalized_2d? | ❌ Only `normalized_2d`. Physical domain requires real dimensions. | **Yes** |
| 5 | Do we have physical ε or only demonstrative? | ⚠️ Partial. `epsilon_r_Si = 11.7` exists in `units.py` but solver uses ε=1. | **Yes** |
| 6 | Do we have sanity checks for φ? | ❌ No. Expected φ range for Si/SiO₂ interface not documented. | **Yes** |
| 7 | Do we have surface-mode vs volume-smearing-mode separation? | ❌ No. `geometry_mode` enum exists but solver doesn't distinguish. | Partial |
| 8 | Do we have a mechanism to prevent overclaim? | ✅ Yes. Metadata guards, scale_modes, quality gates. | No |
| 9 | Do we have RED tests defined? | ❌ No. TDD plan for Option C does not exist yet. | **Yes** |
| 10 | Do we have an ADR sufficient to authorize code? | ❌ No. ADR-008 authorized Option B only. Option C requires new ADR. | **Yes** |

**Blocking items: 8 out of 10.**

---

## Preliminary Verdict

### Option C is NOT ready for implementation.

Option C **is** ready for a dedicated ADR proposal (ADR-009).

The next step must be:

1. **ADR-009 Proposed** — defining the scope, equations, parameters,
   sign conventions, geometry modes, and acceptance criteria for
   Option C physical charge mapping.

2. **NOT code.** No `src/` changes until ADR-009 is Accepted via
   council deliberation and acceptance gates.

### Justification

- 8 of 10 readiness questions are blocking.
- The dimensional chain from D_it to φ has 6 unresolved gaps.
- No TDD plan exists for Option C.
- No ADR authorizes Option C implementation.
- The `scope-guardrails` skill explicitly requires ADR before code.
- The `tdd-red-green-release` skill requires RED tests before GREEN.
- The `physics-dimensional-audit` skill identifies dimensional gaps
  that must be resolved in documentation before implementation.
