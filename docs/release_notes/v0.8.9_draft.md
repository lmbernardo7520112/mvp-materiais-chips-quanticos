# v0.8.9 — One-way Poisson Coupling GREEN Minimum

## Summary

Implements the minimal metadata-only adapter for the one-way Poisson coupling contract, converting v0.8.8 RED to GREEN.

## RED to GREEN Transition

* v0.8.8 RED baseline: 15 failed, 5 passed.
* v0.8.9 GREEN: 20/20 passed.
* Full suite: 333 passed.

## Implementation

* `OneWayPoissonCouplingMetadata` — dataclass with enforced safety defaults.
* `PoissonInputSource` — adapter data container.
* `build_poisson_input_source()` — builder with mandatory validation.
* `validate_no_physical_phi_claim()` / `validate_no_solver_runtime_coupling()` — safety validators.

## Safety Invariants

* `physical_phi_allowed = False` (enforced at construction).
* `solver_invocation_allowed = False` (enforced at construction).
* `calibration_status = "not_calibrated"` (enforced at construction).
* `device_prediction_enabled = False` (enforced at construction).
* `interpretation_status = "demonstrative_only"` (always).
* `physical_interpretation = False` (always).

## Explicitly Not Included

* No Poisson runtime or solver execution.
* No physical phi, dummy phi, or potential grid.
* No calibration claims or device prediction.
* No new dependencies.
* No paid API, external SDK, or goal-like autonomous execution.

## Policy Exception

* `one_way_poisson_coupling.py` added to `authorized_files` (same pattern as `poisson_solver_2d.py`).
* Policy extension ratified via HDL-017.

## Next Step

Merge and tag require explicit human approval after external audit.
